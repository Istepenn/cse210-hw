using System;

class Program
{
    static void Main(string[] args)
    {
        // Function call
        int result = Sum(5, 3);
        Console.WriteLine("Sum: " + result);
    }

    // Function definition
    static int Sum(int a, int b)
    {
        return a + b;
    }
}
